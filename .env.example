# Public site origin — used by sitemap/robots/canonical
SITE_URL=https://bridvance.example

# Plan 4 (form): not needed for Plan 1
CONTACT_TO=
RESEND_API_KEY=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
TURNSTILE_SECRET_KEY=
NEXT_PUBLIC_TURNSTILE_SITE_KEY=

# Enquiry delivery. CONTACT_PHONE is server-only on purpose: no NEXT_PUBLIC_
# prefix, read solely inside app/api/contact/route.ts, so Next cannot inline it
# into a client bundle. Never put a real number in this file — it is committed.
CONTACT_FROM=
CONTACT_PHONE=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM=
